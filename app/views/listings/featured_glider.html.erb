<%
# Set up image glider
options = {
  'size' => 3,
  'image_width' => 293,
  'image_height' => 170,
  'images_per_object' => 3,
  'image_placeholders' => true
}
%>
<%= glider_js('featuredImages', @featured_listings, options) %>
<%= glider_html('featuredImages', @featured_listings, options) %>

