<%
my_broker = discern_contact(listing)
agent_info = []
unless my_broker.user.blank?
  agent_name = []
  agent_name << my_broker.user.first_name
  agent_name << my_broker.user.last_name
  agent_info << agent_name
  agent_info << my_broker.mini_biography
end
%>
<% unless agent_info.blank? %>
  <div class="section agent-info clearfix">
    <div id="agent-layer0">
      <% link_to(listing_path(liaison, :anchor => 'information_request')) do %>
        <%= image_tag 'philhemio(1)_t.jpg', :class=> 'agent-photo' %>
      <% end %>
      <strong>Agent info:</strong> <%= agent_info[1] %>
    </div>
    <% unless controller.controller_name == 'listings' &&
        controller.action_name == 'show' %>
      <div class="agent-name">
        Contact:
        <%= link_to agent_info[0].join(' ') + '<div class="after-icon"></div>',
          listing_path(liaison, :anchor => 'information_request'),
          :target => 'new',
          :class => 'contact-link' %>
        &nbsp;<span id="agent-trigger0" class="show-more">
          <%= image_tag 'right_arrow_black.png' %> <span>show agent info</span>
        </span>
      </div>
    <% end %>
  </div>
<% end %>
