<h1>Editing agent</h1>

<% labelled_form_for(@agent) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => 'agent_fields', 
    :locals => {
      :f => f
    } %>
  
  <div class="row">
    <%= f.submit 'Update' %>
  </div>
<% end %>

<%= link_to 'Show', agent_path(@agent) %> |
<%= link_to 'Back', agents_path %>
