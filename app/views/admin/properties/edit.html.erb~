<h1>Editing property</h1>

<% labelled_form_for(@property) do |f| %>
  <%= f.error_messages %>
  
  <%= render :partial => 'property_fields', 
    :locals => {
      :f => f
    } %>
    
  <div class="row">
    <%= f.submit 'Update' %>
  </div>
<% end %>

<%= render :partial => 'associated_listings' %>

<%= link_to 'Add listing', new_property_listing_path(@property) %> |
<%= link_to 'Show', @property %> |
<%= link_to 'Back', properties_path %>
