<nada />

<%
# TODO: determine selected/checked values for all fields
checked = false
%>

<div class="row">
  <div class="clearfix">
    Province:
    <% lists[:zones].each_with_index do |zone, i| %>
      <% id = 'province_or_zone_' + (i+1).to_s %>
      <div class="inline-form-element">
        <%= radio_button_tag :province_or_zone, zone[1], checked, :id => id %>
        <%= label_tag :province_or_zone, zone[0], :for => id %>
      </div>
    <% end %>
  </div>
  <div class="clearfix">
    Zone:
    <% lists[:provinces].each_with_index do |province, j| %>
      <% id = 'province_or_zone_' + (j + lists[:zones].length + 1).to_s %>
      <div class="inline-form-element">
        <%= radio_button_tag :province_or_zone, province[1], checked, :id => id %>
        <%= label_tag :province_or_zone, province[0], :for => id %>
      </div>
    <% end %>
  </div>
  <%= f.select :barrio_id, lists[:barrios] %>
</div>
<div class="row">
  <%= f.text_field :name %>
</div>
<div class="row">
  <%= f.select :primary_category_id,
    lists[:categories],
    { :label_text => 'Primary category' } %>
</div>
<div class="row">
  <label for="category_ids">Other categories</label>
  <%= multiple_select 'property',
    'category_ids',
    lists[:categories],
    { :selected_items => @selected[:category_ids],
      :outer_class => 'multiple_select',
      :inner_class => 'multiple_select_checkbox' 
    } %>
</div>
<div class="row">
  <%= f.select :primary_style_id,
    lists[:styles],
    { :label_text => 'Primary style' } %>
</div>
<div class="row">
  <label for="style_ids">Other styles</label>
  <%= multiple_select 'property',
    'style_ids',
    lists[:styles],
    { :selected_items => @selected[:style_ids],
      :outer_class => 'multiple_select',
      :inner_class => 'multiple_select_checkbox' 
    } %>
</div>
<div class="row">
  <label for="feature_ids">Features</label>
  <%= multiple_select 'property',
    'feature_ids',
    lists[:features],
    { :selected_items => @selected[:feature_ids],
      :outer_class => 'multiple_select',
      :inner_class => 'multiple_select_checkbox'
    } %>
</div>
<div class="row">
  <%= f.text_field :stories %>
</div>
<div class="row">
  <%= f.text_field :garage_spaces %>
</div>
<div class="row">
  <%= f.text_field :parking_spaces %>
</div>
<div class="row">
  <%= f.text_field :bedroom_number %>
</div>
<div class="row">
  <%= f.text_field :bathroom_number %>
</div>
<div class="row">
  <%= f.text_field :construction_size %>
</div>
<div class="row">
  <%= f.text_field :land_size %>
</div>
<div class="row">
  <%= f.text_field :year_built %>
</div>
<div class="row">
  <%= f.date_select :date_available %>
</div>
<div class="row">
  <%= f.text_area :description %>
</div>
